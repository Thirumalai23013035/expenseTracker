# Requires local PostgreSQL schema named `ledger`.
# See docs/environment-and-database-rules.md

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ledger
    username: ledger_user
    password: ledger_pass

  jpa:
    hibernate:
      ddl-auto: validate
